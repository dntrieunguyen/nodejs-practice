extends layouts/main-layout.pug

block content 
        if users.lenght > 0
            h1 Users
            ul 
                each user in users 
                    li #{user.name}
        else 
            h1 No User Found !